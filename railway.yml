   version: '1.0'
   services:
     django:
       build:
         dockerfile: Dockerfile
       env:
         PYTHONUNBUFFERED: 1
       routes:
         - http://localhost:8000
     telegram-bot:
       build:
         dockerfile: Dockerfile
       env:
         PYTHONUNBUFFERED: 1
       routes:
         - http://localhost:5000